<!DOCTYPE html>
<html lang="en" class="h-full">
  <head>
    <title><%= content_for(:title) || "Doctrine" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Doctrine">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Updated Font Link: Added Fira Code for monospace text %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Fira+Code:wght@400;500&family=Rajdhani:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <%= favicon_link_tag "logo.jpg" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <%# APPLIED THEME HERE: %>
  <%# 1. bg-metal-dark: Sets the dark background color. %>
  <%# 2. text-cyan-100: Sets the default text color to glowing pale cyan. %>
  <%# 3. font-sans antialiased: Sets the default technical font. %>
  <body class="h-full bg-metal-dark text-cyan-100 font-sans antialiased">

    <%# REMOVED: The old <nav> and <main container...> wrappers. %>
    <%# The landing page view is designed to be full-screen, so we yield directly into the body. %>
    <%= yield %>

    <%= audio_tag "sample_doctrine_ambient_1.mp3", autoplay: true, loop: true, class: "hidden" %>

  </body>
</html>
